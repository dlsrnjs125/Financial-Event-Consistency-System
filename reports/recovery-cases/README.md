# Recovery Case Evidence Samples

This directory keeps small curated PH4 recovery case and quarantine examples.

Runtime recovery case exports under `reports/recovery-cases/rc-*/` are ignored by git because they may contain environment-specific operational evidence paths. Do not store raw account numbers, raw idempotency keys, request bodies, HMAC signatures, authorization headers, or secrets here.
