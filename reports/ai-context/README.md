# PH6 AI-safe Context Samples

This directory keeps curated PH6 AI-safe context examples.

Runtime results under `reports/ai-context/run-*/` are ignored by git. PH6 artifacts must not include raw account numbers, raw idempotency keys, request bodies, HMAC signatures, authorization headers, cookies, passwords, client secrets, or database connection strings.
