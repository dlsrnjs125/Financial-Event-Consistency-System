# PH5 Reconciliation Evidence Samples

This directory keeps small curated PH5 reconciliation examples.

Runtime results under `reports/reconciliation/run-*/` are ignored by git because they may contain environment-specific operational evidence paths. PH5 artifacts must not include raw account numbers, raw idempotency keys, request bodies, HMAC signatures, authorization headers, or secrets.
